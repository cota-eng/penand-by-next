import { TAG } from "../types/tag";
interface CONSTTAG {
  id: string;
  name: string;
  slug: string;
  emoji: string;
}
export const tags: CONSTTAG[] = [
  {
    id: "01",
    name: "wood",
    slug: "wood",
    emoji: ":wood:",
  },
];
